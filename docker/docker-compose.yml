version: '3.4'

services:
  profile:
    image: fluffyspoon.profile
    build:
      dockerfile: src/fluffyspoon.profile/Dockerfile
      context: ..
    environment:
      - ASPNETCORE_URLS=http://+:8080
      - Orleans__MembershipProvider__Address=http://consul:8500
    depends_on:
      - consul

  consul:
    image: consul:latest
    command: "agent -server -bootstrap -ui -client 0.0.0.0"
    ports:
      - "8500:8500"